const state = {
  user: {
    name: "Aliakbar Ghasemi",
    avatar:
      "https://avatars.githubusercontent.com/u/33986952?s=40&v=4",
  },
};

const getters = {
  getUser: (state) => state.user,
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  actions,
  mutations,
};
